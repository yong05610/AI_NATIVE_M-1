# PRD: NVIDIA 최근 2년 주가 시계열 분석 프로젝트

## 1. 프로젝트 개요

본 프로젝트는 `yfinance`를 이용해 Yahoo Finance의 NVIDIA Corporation(`NVDA`) 최근 2년 일별 주가 데이터를 수집하고, 추세·수익률·변동성·거래량·계절성 가능성을 분석하는 프로젝트다.

분석 과정과 결과는 VSCode의 Jupyter Notebook에서 재현할 수 있어야 하며, 최종 결과는 `REPORT.md`에 데이터 기반으로 정리한다. 보너스 과제로 Streamlit 대시보드와 Naive Forecast 및 Moving Average Forecast 기반의 간단한 단기 예측을 구현한다.

> 이 프로젝트는 교육용 데이터 분석이며 투자 자문이나 매수·매도 추천을 목적으로 하지 않는다.

## 2. 분석 대상 및 범위

- 종목명: NVIDIA Corporation
- 티커: `NVDA`
- 데이터 출처: Yahoo Finance (`yfinance` 사용)
- 분석 기간: 실행일을 기준으로 최근 2년
- 데이터 주기: 거래일 기준 일별 데이터
- 최소 데이터 수: 100개 이상
- 주요 원본 컬럼: `Open`, `High`, `Low`, `Close`, `Adj Close`, `Volume`
- 개발 환경: VSCode + Jupyter Notebook, Python 3.10 이상

`yfinance` 버전 및 `auto_adjust` 설정에 따라 `Adj Close`가 제공되지 않을 수 있다. 이 경우 실제 수집된 컬럼과 조정주가 적용 여부를 확인하고, 분석에 사용한 가격 컬럼과 설정을 `REPORT.md`에 명시한다.

## 3. 프로젝트 목표

1. `NVDA` 최근 2년 주가 데이터를 재현 가능한 코드로 수집하고 CSV로 저장한다.
2. 날짜, 중복, 결측치, 이상치를 점검하고 처리 기준을 문서화한다.
3. 이동평균, 수익률, 구간별 집계, 이동 변동성 등 기본 시계열 분석 기법을 2개 이상 적용한다.
4. 핵심 분석 질문을 3개 이상 정의하고 각 질문에 데이터와 시각화로 답한다.
5. 최소 3개 이상의 시각화를 만들고 그래프에서 확인한 사실과 해석을 구분한다.
6. Fact-Why-Action 구조의 주요 인사이트를 3개 이상 작성한다.
7. AI 사용 과정과 검증 방법을 투명하게 기록한다.
8. 보너스 과제로 Streamlit 대시보드와 간단한 단기 예측을 구현한다.

## 4. 핵심 분석 질문

다음 질문은 반드시 분석 전에 정의하고 `REPORT.md`에도 그대로 포함한다.

1. 최근 2년간 NVIDIA 주가의 주요 상승/하락 추세 및 전환점은 언제 발생했는가?
2. 급등/급락 구간에서 20일 이동 변동성과 거래량은 어떤 패턴을 보이는가?
3. 월별/분기별 수익률 패턴에 뚜렷한 특징, 즉 계절성이나 특정 시기 반복 패턴이 존재하는가?

선택 분석 질문은 다음과 같다.

4. 20일 이동평균과 60일 이동평균의 관계는 추세 변화 구간을 설명하는 데 어떤 도움을 주는가?
5. Naive Forecast와 20일 Moving Average Forecast 중 최근 30거래일에서 오차가 더 작은 기준 모델은 무엇인가?

### 분석 질문과 검증 방법 연결

| 분석 질문 | 핵심 지표 | 주요 그래프/분석 방법 |
|---|---|---|
| 질문 1: 추세와 전환점 | 종가 변화율, 누적 수익률, MA20, MA60, 고점·저점 날짜 | 종가 및 이동평균 선 그래프, 구간별 변화율 |
| 질문 2: 급등·급락 구간 | 일별 수익률, 20일 이동 변동성, 거래량, 평균 거래량 대비 배수 | 수익률·변동성 그래프, 거래량 그래프, 급등·급락일 표 |
| 질문 3: 반복 패턴 | 월별 수익률, 분기별 수익률, 기간별 평균·중앙값 | 월별/분기별 수익률 막대그래프 및 요약표 |
| 질문 4: 이동평균 관계 | MA20, MA60, 이동평균 간 차이 | 종가와 MA20·MA60 비교 그래프 |
| 질문 5: 기준 예측 비교 | MAE, RMSE | 실제값과 예측값 비교 그래프 및 평가지표 표 |

분석 기간이 2년에 불과하므로 질문 3의 결과는 장기적인 계절성의 확증이 아니라 제한된 표본에서 나타난 반복 가능성으로 해석한다.

## 5. 데이터 수집 및 저장 요구사항

- `yfinance`로 `NVDA` 데이터를 다운로드한다.
- 실행 시점 기준 최근 2년이 되도록 조회 기간 또는 시작일·종료일을 기록한다.
- 조회 기간과 실제 데이터의 첫 거래일·마지막 거래일을 구분해 기록한다.
- 데이터가 100행 이상인지 확인한다.
- 수집 결과를 `data/nvda_2y.csv`에 저장한다.
- 출처, 수집일, 티커, 조회 옵션(`period`, `interval`, `auto_adjust` 등)을 Notebook과 `REPORT.md`에 기록한다.
- 원본 데이터 또는 데이터 수집 코드를 저장소에 포함해 재현 가능하게 한다.

## 6. 데이터 전처리 및 품질 기준

1. 날짜 정제
   - `Date` 컬럼을 올바른 날짜 형식으로 변환하고 오름차순 정렬한다.
   - 중복 날짜를 확인하고 원인을 확인한 뒤 중복 행을 제거한다.
   - 주말과 휴일에 데이터가 없는 것은 정상적인 거래일 특성인지 구분한다.
2. 결측치 처리
   - 컬럼별 결측치 개수와 비율을 확인한다.
   - 핵심 컬럼의 결측은 원인과 규모를 확인한 뒤 제거 또는 보간 여부를 결정한다.
   - 주가 흐름을 왜곡할 수 있으므로 자동 보간을 기본값으로 사용하지 않는다.
   - 이동 계산 초기에 생기는 결측치는 window 부족으로 발생한 정상 결측으로 별도 설명한다.
3. 이상치 검토
   - 일별 수익률 절댓값이 큰 날짜와 거래량 급증 날짜를 확인한다.
   - 급등·급락 값이 실제 시장 움직임, 주식분할/조정주가 효과 또는 데이터 오류인지 원본과 비교한다.
   - 실제 시장 이벤트로 판단한 값은 임의 제거하지 않고 분석 대상으로 유지한다.
4. 시계열 특성 확인
   - 추세, 변동성 변화, 노이즈, 계절성 가능성을 확인한다.
   - 결측치·이상치 판정 기준, 처리 결과, 처리 전후 행 수를 `REPORT.md`에 설명한다.

## 7. 분석 방법

### 7.1 종가 및 추세 분석

- 최근 2년 종가 흐름과 전체 기간 수익률을 확인한다.
- 최고가·최저가 및 주요 상승·하락 구간의 날짜와 변화율을 계산한다.
- 20일 이동평균(`MA20`)과 60일 이동평균(`MA60`)으로 단기·중기 추세를 비교한다.
- 전환점은 이동평균 교차만으로 확정하지 않고 가격 방향과 구간 수익률을 함께 확인한다.

### 7.2 수익률 분석

- 일별 수익률은 `Close.pct_change()`로 계산한다.
- 월별·분기별 수익률은 각 기간의 첫 종가와 마지막 종가 또는 복리 누적 방식으로 계산한다.
- 집계 방법을 명시하고 일별 수익률 평균과 기간 수익률을 혼동하지 않는다.

### 7.3 변동성 및 거래량 분석

- `Volatility_20`은 일별 수익률의 20일 이동 표준편차로 계산한다.
- 연율화하는 경우 `sqrt(252)`를 곱하고 연율화 여부를 축 라벨과 보고서에 표시한다.
- 급등·급락일의 수익률, 변동성, 거래량을 평상시 평균 또는 20일 평균 거래량과 비교한다.

### 7.4 계절성 가능성 분석

- 월별·분기별 수익률을 집계하고 양(+)·음(-)의 수익률이 반복되는지 확인한다.
- 2년 표본만으로 통계적 계절성을 단정하지 않으며 관찰된 패턴과 한계를 함께 기술한다.

## 8. 시각화 요구사항

최소 3개 이상을 만들며 다음 4개 그래프를 권장한다.

1. 최근 2년 종가 추세 그래프
2. 종가와 20일/60일 이동평균 그래프
3. 월별 및/또는 분기별 수익률 그래프
4. 20일 이동 변동성과 거래량 그래프

보너스 예측을 수행하면 실제값과 예측값 비교 그래프를 추가한다. 모든 그래프는 제목, 축 라벨, 단위, 범례를 명확히 표시하고 `images/` 폴더에 PNG로 저장한다. `REPORT.md`에는 정상 작동하는 상대 경로로 삽입하고 각 그래프 아래에 분석 질문과 연결된 설명을 작성한다.

## 9. 주요 인사이트 작성 기준

그래프를 단순 나열하지 않고 주요 인사이트를 최소 3개 이상 작성한다. 각 인사이트는 관찰과 해석을 섞지 않고 다음 구조를 모두 포함해야 한다.

- **Fact:** 날짜, 기간, 가격 변화율, 수익률, 거래량, 변동성 등 데이터로 직접 확인한 사실
- **Why:** 해당 현상의 가능한 원인 또는 해석을 가설로 작성하며, 확인하지 않은 원인은 사실처럼 단정하지 않음
- **Action:** 투자자, 분석가 또는 의사결정자가 참고할 수 있는 시사점, 위험 관리 방향 또는 후속 분석 제안

예시 형식은 다음과 같다.

```text
Insight 1.
- Fact: 특정 기간 동안 종가가 약 XX% 상승했고 거래량이 직전 20일 평균 대비 XX% 증가했다.
- Why: AI 반도체 수요 기대감 또는 실적 발표의 영향일 가능성이 있으나 외부 자료 확인이 필요하다.
- Action: 급등 구간을 평가할 때 수익률뿐 아니라 거래량과 변동성 확대 여부도 함께 확인한다.
```

Fact는 Notebook에서 재계산 가능한 수치에 근거해야 한다. Why에서 뉴스·실적·금리 등을 언급할 때는 가설임을 표시하고, 실제 원인으로 주장하려면 신뢰할 수 있는 출처와 날짜를 별도로 확인한다. Action은 수익을 보장하는 투자 지시가 아니라 분석상 시사점으로 작성한다.

## 10. 보너스 기능 A: 간단 예측

복잡한 모델 대신 이해하기 쉬운 두 기준 모델을 비교한다.

1. Naive Forecast
   - 다음 거래일 종가는 직전 거래일 종가와 같다고 가정한다.
2. Moving Average Forecast
   - 예측 시점 이전 최근 20거래일 평균 종가를 다음 값으로 사용한다.

- 마지막 30거래일을 테스트 구간으로 분리한다.
- 미래 데이터가 계산에 포함되지 않도록 시간 순서를 지킨다.
- MAE와 RMSE로 두 모델을 평가한다.
- 실제 종가와 예측 종가를 같은 그래프에서 비교한다.
- 목적은 기준 모델의 가정, 계산, 오차와 한계를 이해하는 데 있다.

## 11. 보너스 기능 B: Streamlit 대시보드

- 날짜 범위 선택
- 선택 기간의 종가 추세 표시
- 20일/60일 이동평균 표시 옵션
- 일별 수익률, 20일 이동 변동성, 거래량 표시
- 평균, 최댓값, 최솟값, 기간 수익률 등 기초 통계 표시
- 필터 변경 시 그래프와 통계가 함께 갱신되도록 구성
- 실행 방법: `streamlit run app.py`

## 12. AI 사용 로그 작성 기준

AI를 사용한 경우 `REPORT.md`의 **AI 사용 로그**에 다음 3가지 요소를 모든 작업별로 기록한다.

1. **사용 작업**
   - 예: 분석 질문 정리, 데이터 전처리, 코드 작성, 시각화, 오류 해결, 인사이트 정리, 문장 다듬기
2. **사용 이유**
   - 예: 시간 절감, 코드 오류 해결, 대안 탐색, 설명 구조화
3. **검증 방법**
   - 예: 원본 데이터와 비교, 샘플 행 확인, 코드 재실행, 수식 검토, 다른 방법과 결과 비교

권장 형식은 다음과 같다.

| 사용 작업 | 사용 이유 | 검증 방법 |
|---|---|---|
| 전처리 코드 초안 작성 | 구현 시간 절감 및 누락 방지 | 원본 행 수와 처리 후 행 수 비교, 샘플 행 확인, Notebook 전체 재실행 |

AI 생성 결과를 그대로 사용하지 않고, 코드는 실행 결과와 수식을 확인하며 데이터 값은 원본과 대조한다. 해석은 그래프와 계산 수치가 뒷받침하는지 확인하고 최종 판단은 작성자가 수행한다.

## 13. `REPORT.md` 권장 목차

각 분석 질문에 대한 데이터 기반 답변을 명확히 포함한다.

```text
# NVIDIA 최근 2년 주가 시계열 분석 보고서

## 1. 프로젝트 개요
## 2. 분석 질문
## 3. 데이터 설명
## 4. 데이터 전처리
## 5. 분석 방법
## 6. 시각화 결과
## 7. 주요 인사이트
## 8. 간단 예측 결과
## 9. Streamlit 대시보드
## 10. AI 사용 로그
## 11. 결론 및 한계점
```

### 결론 및 한계점 필수 내용

- 주가 데이터만 사용한 분석이므로 기업 가치나 시장 전체 상황을 충분히 설명하지 못하는 한계
- 거시경제 변수, 실적 발표, 뉴스, 금리 등 외생 변수를 반영하지 않은 한계
- Naive 및 Moving Average 기반 단기 예측 모델이 급변과 외부 충격을 반영하기 어려운 한계
- 2년이라는 짧은 기간으로 계절성이나 반복 패턴을 일반화하기 어려운 한계
- 과거 패턴과 백테스트 결과가 미래 수익률을 보장하지 않는다는 점

## 14. 사용 기술 및 재현성

- Python 3.10 이상, VSCode, Jupyter Notebook
- `pandas`, `numpy`, `matplotlib`, `seaborn`
- `yfinance`, `scikit-learn`, `streamlit`

`requirements.txt`에 라이브러리와 버전을 기록하고, `README.md`에 가상환경 설정, 설치, Notebook 실행 순서, Streamlit 실행 명령을 작성한다. Notebook은 위에서 아래로 전체 실행했을 때 같은 결과를 재현할 수 있어야 한다.

## 15. 최종 산출물

```text
nvidia-stock-analysis/
|-- data/
|   |-- nvda_2y.csv
|-- images/
|   |-- 01_close_trend.png
|   |-- 02_moving_average.png
|   |-- 03_monthly_quarterly_return.png
|   |-- 04_volatility_volume.png
|   |-- 05_forecast_comparison.png
|-- analysis.ipynb
|-- app.py
|-- REPORT.md
|-- README.md
|-- requirements.txt
|-- PRD.md
|-- TASK.md
|-- CHECK_LIST.md
```

## 16. 완료 및 품질 기준

- 최근 2년 `NVDA` 일별 데이터가 100개 이상 수집되어 있다.
- 데이터 수집 및 분석 과정이 재현 가능하다.
- 분석 질문이 3개 이상이고 각 질문에 데이터 기반 답변이 있다.
- 결측치·이상치 처리 기준과 결과가 `REPORT.md`에 설명되어 있다.
- 이동평균, 수익률, 변동성 등 시계열 분석 기법이 2개 이상 적용되어 있다.
- 시각화가 최소 3개 이상이며 파일과 보고서 링크가 정상 작동한다.
- Fact-Why-Action 구조의 인사이트가 3개 이상 있다.
- AI 사용 로그에 사용 작업, 사용 이유, 검증 방법이 포함되어 있다.
- 결론 및 한계점에 단일 주가 데이터, 외생 변수 미반영, 단기 예측 모델의 한계와 미래 수익률 비보장 내용이 있다.
- 보너스 기능을 수행한 경우 Streamlit 실행과 예측 계산이 오류 없이 동작한다.
